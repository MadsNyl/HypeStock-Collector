CREATE TABEL article_symbol.db (
    symbol varchar(5),
    article_id int,
    PIRMARY KEY(symbol, article_id),
    FOREIGN KEY(symbol) REFERENCES stock(symbol),
    FOREIGN KEY(article_id) REFERENCES article(id)
)