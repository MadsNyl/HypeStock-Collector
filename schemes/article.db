CREATE TABLE article (
    id int AUTO_INCREMENT,
    provider varchar(255),
    external boolean,
    title varchar(255),
    url varchar(255),
    body text,
    created_date datetime,
    PRIMARY KEY(id)
)